# AccessCard 门禁插件

适用于 **Minecraft 1.20.1 Paper** 的门禁系统。支持**密码门**与**门禁卡门**两类交互，带冷却、尝试次数限制、供电系统和延迟指令链。

- **版本**：1.1.1
- **适用服务端**：Paper 1.20.1（Spigot 系亦可）
- **依赖**：无需任何前置插件（PlaceholderAPI / 多世界插件为可选软依赖）
- **版权**：Keran Technology © 2026 · http://tech.keran.cc
- **下载**：[GitHub Releases](https://github.com/HifumiDaisuki01/AccessCard/releases/latest)

---

## 一、安装

1. 把 `AccessCard-1.1.1.jar` 丢进 `plugins/` 目录
2. 重启服务端（或 `/reload confirm`）
3. 生成目录 `plugins/AccessCard/`，内含：
   - `config.yml` — 全局配置（提示文本、默认冷却、密码超时等）
   - `doors.yml` — 每个门的独立记录（类型、密码、坐标、冷却参数、指令链）

---

## 二、快速上手

```
# 1. 创建一个密码门，密码 abc123
/acd create pw 东大门 abc123

# 2. 看向要当门禁的按钮，执行绑定
/acd bind 东大门

# 3. 完成。玩家右键那个按钮 → 聊天框输入 abc123 → 开门
```

门禁卡门：

```
# 创建门禁卡门，卡名必须包含「钥匙卡」三个字
/acd create card 仓库门 钥匙卡

# 看向按钮绑定
/acd bind 仓库门
```

---

## 三、命令一览

| 命令 | 说明 | 权限 |
|---|---|---|
| `/acd create pw <名称> [密码]` | 创建密码门，不填密码则随机生成 | `accesscard.create` |
| `/acd create card <名称> [关键字]` | 创建门禁卡门 | `accesscard.create` |
| `/acd bind <名称>` | 看向一个按钮执行，把门绑定上去 | `accesscard.bind` |
| `/acd open <名称> [玩家]` | 强制开门，供第三方插件调用 | `accesscard.open` |
| `/acd resetpw <名称> [新密码]` | 重置密码，不填则随机生成字母数字 | `accesscard.resetpw` |
| `/acd randompw <名称> [长度]` | **重置为指定位数的纯数字随机密码**（密室逃脱用） | `accesscard.resetpw` |
| `/acd setpw <名称> <密码>` | 设置指定密码 | `accesscard.resetpw` |
| `/acd power on <名称> [时长]` | 供电，如 `300s` / `5m` / `1h` | `accesscard.power` |
| `/acd power off <名称>` | 切断供电 | `accesscard.power` |
| `/acd power mode <名称> <on\|off>` | 切换该门是否启用电源系统 | `accesscard.power` |
| `/acd info <名称>` | 查看门禁详情 | `accesscard.list` |
| `/acd list` | 列出全部门禁 | `accesscard.list` |
| `/acd remove <名称>` | 删除门禁 | `accesscard.remove` |
| `/acd reload` | 重载配置 | `accesscard.reload` |
| `/acd selftest` | 插件自检（156 项） | `accesscard.reload` |

所有子命令均支持 **Tab 补全**。玩家侧使用权限为 `accesscard.use`（默认所有人）。

---

## 四、指令链配置

在 `doors.yml` 里为每个门写 `commands` 列表。**每行一条，按顺序执行**：

```yaml
doors:
  东大门:
    type: pw
    password: abc123
    commands:
      - "console:setblock 66 66 11 world1 air -2.2s"
      - "console:effect give {player} glowing 20 -0.5s"
      - "tell:你的位置暴露了"
      - "broadcast:{player} 打开了 {door}！"
```

### 支持的指令前缀

| 前缀 | 作用 | 示例 |
|---|---|---|
| `console:` | 以控制台身份执行 | `console:say 门开了` |
| `player:` | 以触发玩家身份执行 | `player:me 我开门了` |
| `tell:` | 私聊触发玩家 | `tell:门已开启` |
| `message:` | 同 `tell:` | `message:欢迎回来` |
| `broadcast:` | 全服广播 | `broadcast:{player} 开门了！` |
| `title:` | 发送标题，`|` 分隔主副标题 | `title:门开了|请注意安全` |
| `actionbar:` | 物品栏上方文字 | `actionbar:门已解锁` |
| `sound:` | 播放音效，`|` 分隔音量与音调 | `sound:BLOCK_NOTE_BLOCK_PLING|1|1` |
| `wait:` | 单纯等待 | `wait:2.5` |
| （无前缀） | 等同 `console:` | `say hello` |

### 延迟语法

**在那行末尾追加 `-Xs` 或 `+Xs`**，表示「本条执行完毕后，延迟 X 秒再执行下一条」：

```yaml
- "console:say 第一步"          # 立即
- "console:say 第二步 -1.5s"    # 第二步执行完后，等 1.5 秒
- "console:say 第三步 -0.5s"    # 第三步执行完后，等 0.5 秒
- "tell:全部完成"               # 最后一条
```

支持小数（`-2.2s`）。不写延迟就是立即执行下一条。`-` 和 `+` 含义相同。

### 占位符

| 占位符 | 替换为 |
|---|---|
| `{player}` | 触发玩家名（控制台触发时为空） |
| `{door}` | 门的展示名 |

---

## 五、冷却机制

每个门可独立配置，`-1` 表示沿用 `config.yml` 的默认值：

```yaml
doors:
  东大门:
    cooldown:
      personal: 30    # 个人冷却：同一个人开过后等 30 秒
      global: 5       # 全局冷却：任何人开过后全服等 5 秒
```

冷却期间交互会提示剩余时间（`个人冷却剩余xx` / `全局冷却xx`）。

---

## 六、密码门的尝试次数

```yaml
doors:
  东大门:
    attempts:
      max: 5          # 允许输错 5 次
      cooldown: 180   # 第 5 次错误后，个人锁定 180 秒
```

**行为**：同一个玩家在同一个门累计输错达到 `max` 次 → 触发个人锁定 `cooldown` 秒 → 锁定期间无法再交互该门 → **锁定结束后次数自动清零**，又恢复完整的 `max` 次机会。

### 输错惩罚（可选）

不配置就完全不生效：

```yaml
    punish-commands:
      - "console:effect give {player} slowness 5 1"
      - "tell:密码错误！"
      - "broadcast:{player} 在 {door} 输错了密码"
```

可用占位符额外包含 `{left}`（剩余次数）、`{used}`（已错次数）、`{max}`（上限）、`{input}`（玩家输入的内容）。

---

## 七、门禁卡门

### 检测规则

1. **物品类型**匹配（可选，配 `card.material`，留空则不限）
2. **名称包含关键字**（大小写不敏感，自动忽略颜色代码）
3. **解析出的次数 > 0**
4. **不检测 lore**

### 次数递减原理

卡名形如 `§7[消耗]§f远航者钥匙卡 §8(剩余次数：10)`。插件采用**多级匹配 + 倒序兜底**策略提取数字：

| 级别 | 匹配形式 | 例子 |
|---|---|---|
| 1 | 带引导词的括号 | `(剩余次数：10)`、`（使用次数:3）`、`(uses 12)` |
| 2 | 任意括号内以数字结尾 | `（10）`、`(x8)` |
| 3 | **倒序扫描，取最后一个连续数字** | `钥匙卡 x12`、`Card - 8` |

命中后**只替换那一段数字**，其余字符（含 `§` 颜色代码、Oraxen/ItemsAdder 的自定义字体乱码）完全原样保留。

> 例：`§7[消耗]§f钥匙卡 §8(剩余次数：10)` → `§7[消耗]§f钥匙卡 §8(剩余次数：9)`

剩余的彩色字符、乱码一个不动，只有数字变了。

配置项：

```yaml
# config.yml
card:
  consume: true        # 刷卡成功后自动 -1
  allow-offhand: false # 是否检测副手
```

次数归零时提示「该卡已损坏」。

---

## 八、供电系统（可选）

给门启用电源后，**默认断电不可用**，交互提示需要恢复供电：

```yaml
doors:
  东大门:
    power:
      enabled: true
```

管理用指令供电，多少秒后自动失效：

```
/acd power on 东大门 300s
```

- 供电期间可正常交互
- 超时后自动断电，再次交互被拒绝
- `power.enabled: false` 的门完全无视供电系统（永久可用）

可用 `/acd power mode <门> on|off` 随时切换某门是否启用电源系统。

### 断电时的提示（可选）

```yaml
    no-power-commands:
      - "tell:这里一片漆黑，似乎没有电力。"
      - "sound:BLOCK_BEACON_DEACTIVATE|1|0.5"
```

---

## 九、第三方插件对接

### 方式一：命令调用

```
/acd open 东大门 Steve
```

指定玩家时会执行完整指令链且 `{player}` 替换为 Steve；**不指定玩家时（控制台调用）依然会执行整条指令链**，`{player}` 替换为空。适合 Quest 插件、命令方块、脚本定时触发。

```
/acd randompw 金库 4           # 把「金库」的密码重置为 4 位随机数字
/acd randompw 金库             # 不写长度则用默认值（默认 4，见 config）
```

### 方式二：PlaceholderAPI 占位符

| 占位符 | 返回 |
|---|---|
| `%acd_doors%` | 门禁总数 |
| `%acd_type_<门>%` | `pw` / `card` |
| `%acd_name_<门>%` | 展示名 |
| `%acd_friendly_<门>%` | `密码门` / `门禁卡门` |
| `%acd_personal_<门>%` | 当前玩家个人冷却剩余（秒） |
| `%acd_global_<门>%` | 全局冷却剩余（秒） |
| `%acd_attempts_<门>%` | 当前玩家已失败次数 |
| `%acd_attemptcooldown_<门>%` | 尝试锁定剩余（秒） |
| `%acd_power_<门>%` | 供电剩余（秒），`-1` 表示未启用电源 |
| `%acd_ready_<门>%` | 当前玩家能否开门：`true` / `false` |
| `%acd_reason_<门>%` | 不能开门的原因 |

`%acd_reason_%` 返回值：`ok`（可以开）、`personal`、`global`、`attempt`、`power`、`offline`。

> 中文门名直接写即可，如 `%acd_ready_东大门%`。

### 方式三：多世界

门记录自带 `world` 字段，天然按世界隔离。同名的按钮方块在不同世界互不干扰。

---

## 十、密室逃脱：密码位占位符

这是为「把密码拆成线索分发」专门做的一组占位符。典型玩法：每局开始时重置密码 → 把 4 位数字分别藏进 4 个谜题 → 玩家集齐后拼出密码开门。

### 占位符一览

| 占位符 | 返回 | 示例（密码 `0421`） |
|---|---|---|
| `%acd_pwd_<门>%` | 完整密码 | `0421` |
| `%acd_pwd_<门>_<位序>%` | 第 N 位，**N 从 1 开始** | `%acd_pwd_金库_2%` → `4` |
| `%acd_pwdlen_<门>%` | 密码位数 | `4` |
| `%acd_pwdlast_<门>%` | 最后一位 | `1` |

要点：

- **首位可以是 0**。`0421` 是合法密码，所以 `%acd_pwd_金库_1%` 会返回 `0`。这样 4 位密码共 10000 种，不会因「首位必非零」而露馅。
- **位序越界返回空串**。密码 4 位时问第 5 位，返回空——可以拿来做「是否还有下一位」的判断。
- **门名可含数字和下划线**。`%acd_pwd_vault1_2%` 会先整体匹配 `vault1_2`，匹配不到再拆成「门 `vault1` + 第 2 位」，所以门名本身带数字也不会冲突。

### 配置开关

```yaml
password-placeholder:
  enabled: true    # false = 全部密码占位符返回空串
  mask: ""         # 填字符（如 -）则只输出该字符，等于只告诉位数不告诉数字
```

`mask` 的用武之地：想让玩家看到「密码是 4 位」但不给具体数字时，设成 `-`，`%acd_pwd_金库_1%` 就只会显示 `-`。

### 完整示例（Quest 插件）

某任务要求玩家找齐 4 块石板，每块石板刻着密码的一位：

```yaml
# Quest / 其他插件的任务文本里
- "石碑上刻着一个数字：%acd_pwd_金库_1%"
- "墙上的涂鸦写着：%acd_pwd_金库_2%"
- "第三块石板已经碎裂，只看得清末尾：%acd_pwdlast_金库%"
```

每局开始前用命令方块或脚本重置密码：

```
/acd randompw 金库 4
```

密码一换，所有线索自动跟着变，不用手工改任何文本。

### 代码方式调用（Java）

第三方插件也可以直接调 API：

```java
DoorService svc = ((AccessCardPlugin) Bukkit.getPluginManager().getPlugin("AccessCard")).getDoorService();

String pw = svc.randomizePassword("金库", 4);   // 重置并拿到新密码，失败返回 null
String d2 = svc.passwordDigit("金库", 2);       // 第 2 位，越界返回 ""
int len   = svc.passwordLength("金库");         // 位数
String all = svc.passwordOf("金库");            // 完整密码
```

### 安全提示

密码占位符默认开启。如果服务器上有不希望被读取密码的场景，把 `password-placeholder.enabled` 设为 `false` 即可整体关闭；也可以设 `mask` 为某个字符，让占位符只输出位数。

同时建议 `random-password.broadcast` 保持 `false`（默认就是 `false`）——开启后新密码会全服广播，密室谜底直接泄露。

---

## 十一、提示前缀与可见范围

如果你服务器上的门禁分属不同「制造商」，这一节能让每个门带自己的招牌语气；也能控制每条提示给谁看。

**两处都是可选的**：门里不写，就自动用 `config.yml` 里的全局设置；全局不写，就用插件内置默认值（前缀为空、所有提示仅开门玩家可见）。

### 11.1 每个门的前缀

给门加一行 `prefix` 就行：

```yaml
doors:
  金库大门:
    type: pw
    password: "8888"
    prefix: "[天穹银行]"
```

效果：

```
[天穹银行] 金库大门已开启
```

不写 `prefix` 的门，走全局前缀（`config.yml` 里的 `announce.prefix`，默认 `[门禁]`）。

### 11.2 每条提示的可见范围

七类提示，每一类都能单独设置可见范围：

| 配置键 | 对应提示 |
|---|---|
| `opened` | 开门成功 |
| `wrong` | 密码错误 |
| `timeout` | 密码输入超时 |
| `locked` | 输错次数用尽被锁定 |
| `cooldown` | 冷却中（个人 / 全局） |
| `noPower` | 门没电，被拒绝 |
| `power` | 供电恢复 / 中断 |

范围有三种写法：

| 写法 | 含义 |
|---|---|
| `player` | 只有开门的那位玩家能看到 |
| `nearby:20` | 按钮 **20 格以内**的玩家（自动限定同一世界，多世界互不串台） |
| `all` | 全服广播 |

门里只写想改的那几条，剩下自动走全局默认：

```yaml
doors:
  金库大门:
    type: pw
    password: "8888"
    prefix: "[天穹银行]"
    announce:
      opened: nearby:20     # 开门提示给周围 20 格的人看
      wrong: all            # 输错密码全服广播（制造紧张感）
```

上面这个门，除了 `opened` 和 `wrong`，其余五类提示（`timeout` / `locked` / `cooldown` / `noPower` / `power`）都还是走全局默认，不用重复写。

> **范围以按钮为中心**。`nearby` 的距离是从**门禁按钮**算起的，不是从玩家算起。所以站在门边的队友能看到，站在大厅另一头的看不到——多人解谜时这个特性很实用。

### 11.3 全局默认

在 `config.yml` 里统一设置，所有没单独配置的门都跟着走：

```yaml
announce:
  prefix: "[门禁]"

  default:
    opened: player      # 开门成功
    wrong: player       # 密码错误
    timeout: player     # 密码输入超时
    locked: player      # 输错次数用尽被锁定
    cooldown: player    # 冷却中（个人 / 全局）
    noPower: player     # 无供电
    power: all          # 供电恢复 / 中断
```

**想让某条提示彻底不出现**，把对应项写成空串 `""` 即可：

```yaml
    cooldown: ""    # 冷却中的提示静默，不打扰玩家
```

### 11.4 想改成别的写法？

`nearby` 除了 `nearby:20`，也接受 `nearby 20`（空格分隔）和裸数字 `20`。另外 `player` 可以写成 `自己`，`all` 可以写成 `全服`，效果完全一样。

写错了不会报错，会自动退回到最安全的 `player`（只有开门玩家可见），不会把提示误广播给全服。

### 11.5 查看某个门的实际配置

`/acd info <门名>` 会把前缀和每条提示的最终生效值都列出来，标了灰色的表示「没单独配置、正在用全局」：

```
提示前缀: [天穹银行]
提示范围: opened=nearby:20 wrong=all timeout=player locked=player cooldown=player noPower=player power=all
```

### 11.6 完整示例：一家虚拟制造商

```yaml
doors:
  金库大门:
    type: pw
    display-name: 金库大门
    password: "8888"
    prefix: "[天穹银行]"
    announce:
      opened: nearby:20
      wrong: all
    commands:
      - "broadcast:金库大门已被打开！"
```

```
[天穹银行] 金库大门已开启        （周围 20 格可见）
[天穹银行] 金库大门密码错误！触发警报！   （全服可见）
```

---

## 十二、完整 doors.yml 示例

```yaml
doors:
  东大门:
    type: pw
    display-name: 东大门
    button:
      world: world
      x: 100
      y: 64
      z: 200
    password: abc123
    prefix: "[一号安保系统]"        # 可选，不写用全局前缀
    announce:                       # 可选，只写想改的那几条
      opened: nearby:20
      wrong: all
    cooldown:
      personal: 30
      global: 5
    attempts:
      max: 5
      cooldown: 180
    power:
      enabled: false
    commands:
      - "console:setblock 100 64 201 redstone_block -2.2s"
      - "console:setblock 100 64 201 air -0.5s"
      - "tell:东大门已开启"
      - "broadcast:{player} 打开了 {door}！"
    punish-commands:
      - "console:effect give {player} slowness 3 1"
    no-power-commands: []

  仓库门:
    type: card
    display-name: 仓库门
    button:
      world: world
      x: 150
      y: 64
      z: 300
    card:
      keyword: 钥匙卡
      material: PAPER
    cooldown:
      personal: 10
      global: 0
    attempts:
      max: 3
      cooldown: 60
    power:
      enabled: true
    commands:
      - "console:playsound minecraft:block.iron_door.open block @a ~ ~ ~ 1 1"
      - "tell:门禁验证通过"
    punish-commands: []
    no-power-commands:
      - "tell:没有电力，门纹丝不动。"
```

`prefix` 和 `announce` 都不写，这个门就完全跟着 `config.yml` 的全局设置走——想省事可以整份文件里一处都不写。

---

## 十三、权限节点

| 权限 | 默认 | 说明 |
|---|---|---|
| `accesscard.use` | 所有人 | 允许使用门禁（输密码 / 刷卡） |
| `accesscard.admin` | OP | 包含下列全部管理权限 |
| `accesscard.create` | OP | 创建门禁 |
| `accesscard.bind` | OP | 绑定按钮 |
| `accesscard.remove` | OP | 删除门禁 |
| `accesscard.open` | OP | 远程开门 |
| `accesscard.resetpw` | OP | 重置密码 |
| `accesscard.power` | OP | 控制供电 |
| `accesscard.list` | OP | 查看列表与详情 |
| `accesscard.reload` | OP | 重载配置与自检 |

---

## 十四、config.yml 关键项

```yaml
defaults:
  personal-cooldown: 0     # 门未单独配置时的默认个人冷却（秒）
  global-cooldown: 0       # 默认全局冷却（秒）
  max-attempts: 5          # 默认允许输错次数
  attempt-cooldown: 180    # 默认锁定时间（秒）

password:
  timeout: 30              # 输入密码超时（秒）
  silent: true             # 输入时不上公屏（强烈建议 true）
  allow-cancel: true       # 允许输入 cancel / 取消 放弃

card:
  consume: true            # 刷卡自动扣次数
  allow-offhand: false     # 是否检测副手

anti-spam:
  interact-throttle-ms: 300  # 同一玩家两次交互最小间隔

announce:
  prefix: "[门禁]"          # 全局前缀，门可单独覆盖
  default:                  # 每条提示的默认可见范围
    opened: player
    wrong: player
    timeout: player
    locked: player
    cooldown: player
    noPower: player
    power: all
```

### 全部提示文本均可自定义

`config.yml` 的 `messages:` 段落包含所有提示语，用 `&` 写颜色代码。带变量的提示支持这些占位符：`{door}` `{player}` `{time}` `{left}` `{used}` `{max}` `{attempt}` `{input}`。

改成纯文本直贴版只需把颜色代码去掉即可。

---

## 十五、编译

需要 **JDK 17** 与 Maven：

```bash
cd 源码
mvn package
# 产物：target/AccessCard-1.1.1.jar
```

源码结构：

```
src/main/java/com/keran/accesscard/
├── AccessCardPlugin.java          主类，生命周期与会话管理
├── command/
│   ├── AcdCommand.java            /acd 全部子命令与 Tab 补全
│   └── SelfTestCommand.java       45 项自检
├── config/
│   └── Messages.java              提示文本与全局默认值
├── door/
│   ├── Door.java                  门数据模型
│   ├── DoorManager.java           注册表、双索引、持久化
│   └── DoorService.java           冷却/供电/尝试次数校验与开门流程
├── listener/
│   ├── CardInteractListener.java  按钮交互分发
│   ├── ChatInputListener.java     密码输入拦截
│   └── PlayerListener.java        退出清理
├── hook/
│   └── AcdPlaceholder.java        PlaceholderAPI 扩展
└── util/
    ├── CardNameParser.java        卡名次数解析（多级+倒序兜底）
    └── CommandChain.java          指令链与延迟调度
```

---

## 十六、自检

上线后执行 `/acd selftest`，会跑 45 项检查覆盖卡名解析、递减边界、按钮识别、时长解析、状态机、配置完整性等，全部通过表示环境正常。

---

## 十七、常见问题

**Q：门绑错按钮了怎么办？**
`/acd bind <门名>` 重新指向正确的按钮即可，会自动解绑旧位置。

**Q：一个按钮能绑两个门吗？**
不能。绑定时如果按钮已被占用会提示，需先改绑或删除原门。

**Q：密码是明文存的吗？**
是。`doors.yml` 里直接写明文，方便随手修改。介意的话可以把服务器文件权限收紧。

**Q：重启后冷却还在吗？**
不在。冷却与供电属于运行时状态，重启即清空。门的配置（密码、指令链、参数）会持久化。

**Q：卡名解析失败了怎么办？**
插件会提示玩家「无法识别剩余次数」并在控制台记录完整卡名。把那个卡名发我，可以针对性加匹配规则。兜底逻辑已经能覆盖绝大多数格式（取最后一个连续数字）。
